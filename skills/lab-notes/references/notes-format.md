# notes.md 형식

```markdown
---
tech: asyncio
slug: asyncio-event-loop
status: cycling               # cycling | done(과제 시험 통과)
task: "API 100개를 동시에 호출해 결과를 모은다"
sessions: [2026-09-08]
---

## 정리                        # 4단계에 쓴다. 그전에는 이 절이 없다. 끊을 때마다 갈아 쓴다
- 의심한 것: "스레드 없이 어떻게 동시에 되나" → 찾은 왜: 기다리는 시간이 대부분인 일에서 스레드는 낭비였다(PEP 3156 의 문제 정의)
- 처음엔 "await 가 스레드를 하나 더 쓴다"고 봤는데, 회차 1에서 순서 출력을 보고 "루프가 하나씩 돌려 준다"로 고쳤다
- 남은 모르겠다: "sleep(0) 말고 다른 await 도 다 돌려주나"

## 줄기
- 추측: "스레드가 무거워서 만든 것 같다. 지금도 그 문제는 있다. 대안은 스레드 풀"
- 출처: PEP 3156 (van Rossum, 2012) — "…" (원문 대목 그대로)
- 희석된 것: "스레드가 무겁다"는 이유는 남아 있지만, 지금은 "취소와 타임아웃을 한곳에서 다루는 것"이 더 큰 이유로 보인다 — 사용자의 낱말
- 남은 것: 기다리는 시간이 긴 I/O 에서 스레드 하나로 여럿을 다루는 것

## 과제
- 하려는 것: API 100개를 동시에 호출해 결과를 모은다

## 지도                        # 회차를 돌며 줄이 는다
| # | 기제 | 숨은 상태 | 관측할 것 | 상태 |
|---|---|---|---|---|
| 1 | 루프는 한 번에 코루틴 하나를 돌린다 | 준비 큐, 지금 도는 태스크 | print 순서 | 됨 |
| 2 | await 는 제어를 루프에 돌려준다 | 대기 중인 future | await 전후 실행 순서 | 도는 중 |

## 회차
### 1. 기제 1 — 2026-09-08
- 실험: `lab/01-order.py`. 코루틴 둘이 각각 print 를 두 번, 사이에 sleep(0)
- 없으면: "루프가 없으면 그냥 함수 호출이라 A 가 다 끝나고 B 가 돈다"
- 예측: "A1 A2 B1 B2. 하나가 끝나야 다음이 돈다" · 높음
- 결과: A1 B1 A2 B2. 준비 큐를 찍은 출력: [B] → [A] → [B]
- 어긋남: 네 가설 "sleep 이 스레드를 바꾼다" → 짚은 것: 스레드는 하나(threading.get_ident 동일), sleep(0) 이 제어를 루프에 돌려주고 큐의 다음을 꺼낸다
- 설명: "코루틴이 await 를 만나면 루프한테 돌아가고, 루프가 큐에서 다음 걸 꺼내 돌린다. 스레드는 하나"
- 되물음: "큐에 들어가는 시점은 언제인가" → "await 가 끝났을 때… 아니 sleep(0) 이면 바로" · "루프가 없으면 무엇이 깨지나" → "돌려줄 곳이 없으니 await 에서 그냥 멈춘다"
- 판정: 맞음 — 숨은 상태(큐, 제어 반환)와 없으면 깨지는 것(돌려줄 곳)이 설명과 되물음에 있다
- 모르겠다: "sleep(0) 말고 다른 await 도 다 돌려주나"
```

`실험`·`없으면`·`예측`·`결과`·`설명`·`되물음`·`판정` 은 회차마다 늘 있고, `어긋남`·`모르겠다` 는 생겼을 때 더한다. 지도의 `상태` 는 `안 함`·`도는 중`·`됨` 셋이다.

**회차 번호는 기술 전체로 이어 붙인다.** 세션이 여럿이라 정리가 번호로 가리킨다.
